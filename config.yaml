paths:
  # Relative paths (workspace-relative, portable across machines)
  db_host_windows: "./kraken_db"
  data_host_windows: "./fastQ_reads"

kraken2:
  image: "staphb/kraken2:latest"
  threads: 8
  confidence: 0.0  # Default: 0.0 (no filtering), higher = more stringent